# API Contract Notes

Initial scaffold routes:

- `GET /health`
- `POST /api/sessions`
- `POST /api/garments/intake`
- `POST /api/tryon/live-fit`
- `POST /api/renders`
- `GET /api/wardrobe`

These are placeholder contracts that we will replace with richer request and response schemas as garment intake and live fitting become real.
